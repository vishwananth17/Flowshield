services:
  # Backend FastAPI Service
  - type: web
    name: flowshield-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:$PORT"
    plan: free
    region: ohio
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: flowshield-db
          property: connectionString
      - key: ENVIRONMENT
        value: production
      - key: RESEND_API_KEY
        sync: false # You will add this manually in the Render dashboard
      - key: CORS_ORIGINS
        value: "*" # We will sharpen this once we have the Vercel link

databases:
  - name: flowshield-db
    plan: free
    region: ohio
